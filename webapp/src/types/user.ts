export type UserDTO = {
  email: string;
  password: string;
  passwordConfirm: string;
  name: string;
};

export type UserInfo = {
  id: number;
  email: string;
  name: string;
  avatarImage: string;
};
